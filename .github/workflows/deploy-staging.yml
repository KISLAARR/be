# Автодеплой staging: пуш в ветку staging → по SSH выполнить deploy.sh staging.
# SSH-ключ на сервере ограничен форс-командой (умеет только deploy.sh staging).
# Прод деплоится отдельным workflow (deploy-prod.yml) из main.
#
# Требуемые secrets (без них джоба тихо пропустится):
#   DEPLOY_HOST     — IP/хост сервера Timeweb
#   DEPLOY_USER     — deploy (создаёт server/bootstrap.sh)
#   DEPLOY_SSH_KEY  — приватный ключ (пара к ключу из bootstrap)
name: deploy-staging

on:
  push:
    branches: [staging]

# Один деплой за раз; новый пуш ждёт завершения предыдущего
concurrency:
  group: deploy-staging
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
    steps:
      - name: Deploy staging over SSH
        if: ${{ env.DEPLOY_HOST != '' }}
        env:
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh -i ~/.ssh/deploy_key \
              -o StrictHostKeyChecking=accept-new \
              -o ConnectTimeout=15 \
              "${DEPLOY_USER}@${DEPLOY_HOST}" \
              'cd /opt/rumi/be && ./deploy.sh staging'

      - name: Skipped (нет DEPLOY_HOST)
        if: ${{ env.DEPLOY_HOST == '' }}
        run: echo "Секреты деплоя не заданы — пропускаем (сервер ещё не подключён)."
